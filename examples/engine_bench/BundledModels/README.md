# Offline dev bundles

Drop an unpacked engine bundle here as `BundledModels/<name>/` (for example
`parakeet-tdt-0.6b-v3/`) to bench it without a Hugging Face download. The
folder is shipped as a folder reference, so it must exist even when empty.
